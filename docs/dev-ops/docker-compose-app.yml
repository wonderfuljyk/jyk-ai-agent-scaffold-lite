# /usr/local/bin/docker-compose -f /docs/dev-ops/environment/environment-docker-compose-2.4.yml up -d
version: '3.8'
# docker-compose -f docker-compose-app.yml up -d
# 你需要修改system为你自身系统的仓库名
services:
  # 部署前端项目
  nginx:
    image: registry.cn-hangzhou.aliyuncs.com/xfg-studio/nginx:latest
    container_name: nginx
    restart: always
    ports:
      - '443:443'
      - '80:80'
    volumes:
      - ./nginx/html:/usr/share/nginx/html
    privileged: true

#  ai-agent-scaffold-lite:
#    image: system/ai-agent-scaffold-lite:1.0
#    container_name: ai-agent-scaffold-lite
#    restart: on-failure
#    ports:
#      - "8091:8091"
#    environment:
#      - TZ=PRC
#      - SERVER_PORT=8091
#    volumes:
#      - ./log:/data/log
#    logging:
#      driver: "json-file"
#      options:
#        max-size: "10m"
#        max-file: "3"
#    networks:
#      - my-network

networks:
  my-network:
    driver: bridge
